# TODO:

* text embed convenience function
* image embed convenience function
* text batch embed function
* ergonomics of general_export_onnx.py (parse arguments I think)
    * `uv run generate_onnx.py --id timm/MobileCLIP2-S4-OpenCLIP`
    * `cargo run --package open_clip --example general_search -- --id timm/MobileCLIP2-S4-OpenCLIP`